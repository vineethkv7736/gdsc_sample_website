// Do not try to pull the changes directly to the main branch in github.

// DO not make any changes to pages other than the one assigned to you.

// All pages should be responsive.

// Maintain a color theme (Avoid multiple color mixing)

// Notify all team members when making changes in style.css, as it can impact all pages.

// Add any extra images exclusively to the /images folder within /assets.

// It's acceptable to include CSS frameworks on your page.
